spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:omega;DB_CLOSE_EXIT=FALSE;
    username: admin
    password: admin

  liquibase:
    contexts: test
    change-log: classpath:liquibase/master.xml
